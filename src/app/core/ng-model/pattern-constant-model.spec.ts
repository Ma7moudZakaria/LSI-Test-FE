import { PatternConstantModel } from "./pattern-constant-model";

describe('PatternConstantModel', () => {
  it('should create an instance', () => {
    expect(new PatternConstantModel()).toBeTruthy();
  });
});